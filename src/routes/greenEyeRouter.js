const exp = require("express")
const router = exp.Router()

router.post("/", () => {})

router.get("/my", () => {})

router.get("/:id", () => {})

router.post("/reply", () => {})

router.delete("/:id", () => {})


module.export = router


